import { Text, View } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context";

const Jobs = () => {
    return (
        <SafeAreaView>
        <Text>Jobs</Text>
    </SafeAreaView>
    );  
};

export default Jobs;